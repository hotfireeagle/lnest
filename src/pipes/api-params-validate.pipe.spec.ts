import { ApiParamsValidatePipe } from './api-params-validate.pipe';

describe('ApiParamsValidatePipe', () => {
  it('should be defined', () => {
    expect(new ApiParamsValidatePipe()).toBeDefined();
  });
});
